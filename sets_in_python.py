# set is mutable means we can add / remove elements but set elements are immutable bcoz we cant update values in the  set'
# set values must be unique and set is unordered
# syntax to define empty set
collection=set()
print(type(collection))

collection={}
print(type(collection))

collection={1,8,9,541,47,12}
print(collection)